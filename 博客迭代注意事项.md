# 博客迭代注意事项（写给接手维护的 AI）

> 项目路径：`D:/bolg/blog`，Hexo 8.1.2 + Butterfly 5.5.5-b1，站点根路径 `/blog/`。
> 本文记录已踩过的坑，修改前请先读完，不要重蹈覆辙。

## 一、最重要的坑（已犯过一次，勿再犯）

### 1. 主题自带的 MetingJS 是老版 v1.2.0，不支持 `<meting-js>` 标签

- 文件实为 `butterfly-extsrc@1.1.6/metingjs/dist/Meting.min.js`，它**只扫描 `class="aplayer"` 的 div**，从 `data-*` 属性读配置（`data-server` / `data-type` / `data-id` / `data-fixed` / `data-mini` / `data-listfolded` 等，属性名全小写）。
- 全局固定播放器必须同时带 `no-destroy` 类。MetingJS 会在首次初始化时自动补上 `no-reload`，从而避免 PJAX 换页后的 `loadMeting()` 销毁并重建播放器；缺少该类会导致音乐每次换页从头加载。
- 网上多数教程写的 `<meting-js ...>` 是 MetingJS v2 的用法，**在本主题里会被静默忽略，播放器完全不渲染，且控制台无报错**。
- 正确写法（已在 `_config.butterfly.yml` 的 `inject.bottom` 中）：
  ```html
  <div class="aplayer" data-server="netease" data-type="playlist" data-id="3778678"
       data-fixed="true" data-mini="true" data-order="random" data-preload="none"
       data-listfolded="true" data-autoplay="false" data-theme="#67d3dc"></div>
  ```
- 同理，对主题任何"第三方功能"，**先下载其实际加载的 JS 源码确认版本和 API**，再写接入代码，不要照搬网络教程。

### 2. hexo server 不会热加载配置文件

- 修改 `_config.yml` / `_config.butterfly.yml` 后**必须重启 hexo server** 才生效。
- 它的文件监听只覆盖 `source/` 和主题文件，且是**增量重建**：改 CSS 只会重新拷贝 CSS，不会重新渲染 HTML。
- 用户本地常驻 4000 端口服务，改完配置请主动重启验证（`hexo server`）。

### 3. 验证必须看"渲染后的 DOM"，不能只看构建日志或静态 HTML

- `hexo generate` 成功 ≠ 功能正常。JS 依赖型功能（播放器、Meting API 数据）在静态 HTML 里只是一个空壳标签。
- 可靠验证流程：
  ```bash
  npx hexo generate                                    # 退出码必须为 0（不要用管道掩盖退出码）
  curl -s http://localhost:4000/blog/ | grep <标记>     # 检查 HTML 注入
  # 无头 Chrome 渲染后检查真实 DOM（机器上有 Chrome）：
  "/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu \
    --no-first-run --timeout=15000 --dump-dom http://localhost:4000/blog/ | grep <渲染后标记>
  ```
- 提醒用户验证前 `Ctrl+F5` 强刷，浏览器缓存是造成"看不到改动"的常见原因。

## 二、项目结构与约定

- **主题配置走 `_config.butterfly.yml`**（站点根目录的覆盖配置），不要直接改 `themes/butterfly/_config.yml`。
- **URL 根路径是 `/blog/`**：`inject` 里的本地资源路径必须带 `/blog/` 前缀（如 `/blog/js/xxx.js`），否则部署后 404。
- 首页分页固定为 **每页 9 篇**：`_config.yml` 的 `index_generator.per_page` 和全局 `per_page` 必须保持一致。
- 自定义代码都放 `source/js/*.js` + `source/css/custom.css`，在 `_config.butterfly.yml` 的 `inject` 中注册。
- 自定义 JS 的统一写法（见 `source/js/home-bg-effects.js`、`scroll-effects.js`）：
  - IIFE 自执行 + `window[stateKey]` 防重复初始化
  - 监听 `DOMContentLoaded` / `load` / `pjax:complete` 三个事件做 refresh
  - 遵守 `prefers-reduced-motion`，移动端/降级场景要兜底
- **PJAX 已开启**，用于保证固定 APlayer 在站内切换页面时持续播放；自定义页面功能必须监听 `pjax:complete` 重新获取新 DOM，内部跳转优先使用 `window.pjax.loadUrl()`，不要直接整页刷新。
- **不要再启用主题自带 `fireworks`**：其脚本即使没有点击也会常驻全屏 Canvas 动画循环，曾造成预加载遮罩结束后帧率骤降。
- 新增趣味组件不得创建空闲时持续运行的 `requestAnimationFrame`；短动画必须只在交互期间运行，并兼容 `prefers-reduced-motion`。

## 三、容易踩的样式坑

- `_config.butterfly.yml` 和 `source/css/custom.css` 是 **CRLF/LF 混合换行**，用编辑工具做文本替换时必须精确匹配换行符，否则匹配失败。
- `#card-toc` 吸顶用的是 `position: fixed !important`（`toc-sticky.js` + custom.css 实现）。**不要给 `.card-widget` 等目录卡片父级加 `transform`/`filter` 动画**——transform 会使 fixed 定位失效。现有滚动渐入代码已用 `.card-widget:not(#card-toc)` 排除，新增动画目标时注意同样的问题。
- 主题色变量：`--cy-accent: #67d3dc`（冰蓝），亮/暗两套由 `html[data-theme='light']` 覆盖，新组件两套都要适配。

## 四、当前已加的功能（2026-07-20）

| 功能 | 位置 |
| --- | --- |
| 全局音乐播放器（APlayer+Meting 老版 div 写法） | `_config.butterfly.yml` `aplayerInject` + `inject.bottom`；样式在 `custom.css` 末尾"音乐播放器主题化"段 |
| 点击烟花（已停用） | `_config.butterfly.yml` `fireworks.enable: false`，不要重新开启 |
| 阅读进度条 + 滚动渐入 | `source/js/scroll-effects.js` + `custom.css` 末尾两段 |
| 星空/雪花背景、日月切换动画、目录吸顶 | `source/js/` 下既有三个文件 |
| 初音天气看板、主题衣橱、页脚状态舱、随机文章扭蛋 | `source/js/fun-widgets.js` + `source/_data/widget.yml` + `scripts/fun-widgets-data.js` |
| 首页贴纸/全息反光、文章标题装饰 | `themes/butterfly/layout/includes/mixins/indexPostUI.pug` + `source/css/custom.css` |

### 趣味组件约定

- 天气接口使用无需密钥的 Open-Meteo；只有用户点击天气卡按钮后才申请浏览器定位权限。
- 定位坐标仅保留两位小数并写入本地缓存 `cy_weather_v1`，天气缓存有效期 30 分钟；拒绝定位或定位失败时回退上海。
- 主题衣橱默认使用樱花粉，缓存键为 `cy_palette_v2`，可选 `miku` / `sakura` / `violet`，必须同时适配亮色和暗色模式。
- `scripts/fun-widgets-data.js` 构建 `/blog/fun-posts.json`，随机文章按钮依赖该文件，修改生成器后要执行完整构建验证。

### 首页置顶文章约定

- 在文章 Front Matter 中设置 `sticky: 正数` 即可进入首页“置顶文章”横向区域，数字越大排序越靠前；相同优先级按发布日期倒序。
- 兼容 `top: true`，其优先级按 `1` 处理；`sticky: 0`、缺省或无效值均视为不置顶。
- 置顶区域只在首页第一页且存在置顶文章时渲染；置顶文章仍保留在下方普通文章列表中。
- 横向区域脚本位于 `source/js/pinned-posts.js`，使用 PJAX 单实例初始化；不得增加自动轮播或空闲时持续运行的动画循环。

## 五、遗留事项

- 播放器当前网易云歌单 ID 为 `18170738222`；替换时修改 `_config.butterfly.yml` 中播放器节点的 `data-id`，并重启 Hexo 服务。
- 截图类验证注意：本站有 preloader，无头浏览器截图容易卡在 Loading 画面，属于截图时机问题，不是功能故障——以 dump-dom 结果为准。
