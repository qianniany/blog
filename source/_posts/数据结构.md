---
title: 数据结构
cover: /img/covers/Kafka.jpg
date: 2025-04-01
---

# 数据结构

# 5\.串

https://www\.bilibili\.com/video/BV1AY4y157yL/?spm_id_from=333\.337\.search\-card\.all\.click\&vd_source=70180bc2f81b0509a2297c22c6f298bd

## kmp算法

```C
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <string.h>
using namespace std;
int main()
{
    char s[20] = "abaabababca";
    char t[20] = "ababc";
    int Next[20] = { 0 };
    int cnt = 0;
    int i = 1;
    while (i < strlen(t)) {
        if (t[cnt] == t[i]) {
            cnt++;
            Next[i] = cnt;
            i++;
        }
        else {
            if (cnt == 0) {
                Next[i] = cnt;
                i++;
            }
            else {
                cnt = Next[cnt - 1];
            }
        }
    }
    for (int i = 0; i < strlen(t); i++) {
        printf("%d ", Next[i]);
    }
    printf("\n");
    i = 0;
    int j = 0;
    while (i < strlen(s)) {
        if (s[i] == t[j]) {
            i++;
            j++;
        } else if(j > 0) {
            j = Next[j - 1];
        } else{
            i++;
        }
        if(j == strlen(t)){
            printf("%d", i - j);
        }
    }
    return 0;
}
```

# 6\.树

## 1\.树的定义

树是n（n\>=0）个结点的有限集。当n = 0时，称为空树。在任意一棵非空树中应满足：

1. 有且仅有一个特定的称为根的结点。

2. 当n\>1时，其余节点可分为m（m\>0）个互不相交的有限集T1,T2,…,Tm，其中每个集合本身又是一棵树，并且称为根的子树

树作为一种逻辑结构，同时也是一种分层结构，具有以下两个特点：

1. 树的根结点没有前驱，除根结点外的所有结点有且只有一个前驱。

2. 树中所有结点可以有零个或多个后继。

![image.png](/img/posts/data-structures/image%2012.png)

## 2\.树的基本术语

1\.考虑结点K。根A到结点K的唯一路径上的任意结点,称为结点K的祖先。如结点B是结点K的祖先,而结点K是结点B的子孙。路径上最接近结点K的结点E称为K的双亲,而K为结点E的孩子。根A是树中唯一没有双亲的结点。有相同双亲的结点称为兄弟,如结点K和结点L有相同的双亲E,即K和L为兄弟。

2\.树中一个结点的孩子个数称为该结点的度,树中结点的最大度数称为树的度。如结点B的度为2,结点D的度为3,树的度为3。

3\.度大于0的结点称为分支结点\(又称非终端结点\);度为0\(没有子女结点\)的结点称为叶子结点\(又称终端结点\)。在分支结点中,每个结点的分支数就是该结点的度。

4\.结点的深度、高度和层次。

结点的层次从树根开始定义,根结点为第1层,它的子结点为第2层,以此类推。双亲在同一层的结点互为堂兄弟,图中结点G与E,F,H,I,J互为堂兄弟。

结点的深度是从根结点开始自顶向下逐层累加的。

结点的高度是从叶结点开始自底向上逐层累加的。

树的高度\(或深度\)是树中结点的最大层数。图中树的高度为4。

5\.有序树和无序树。树中结点的各子树从左到右是有次序的,不能互换,称该树为有序树,否则称为无序树。假设图为有序树,若将子结点位置互换,则变成一棵不同的树。

6\.路径和路径长度。树中两个结点之间的路径是由这两个结点之间所经过的结点序列构成的,而路径长度是路径上所经过的边的个数。

7\.注意:由于树中的分支是有向的,即从双亲指向孩子,所以树中的路径是从上向下的,同一双亲的两个孩子之间不存在路径。

8\.森林。森林是m \(m≥0\)棵互不相交的树的集合。森林的概念与树的概念十分相近，因为只要把树的根结点删去就成了森林。反之，只要给m棵独立的树加上一个结点，并把这m棵树作为该结点的子树，则森林就变成了树。

注意:上述概念无须刻意记忆， 根据实例理解即可。

## 3\.树的性质

树具有如下最基本的性质：

1\.树中的结点数等于所有结点的度数加1\.

2\.度为m的树中第i层上至多有m^\(i\-1\)个结点\.

3\.高度为h的m叉树至多有\( m^h − 1 \) / \( m − 1 \)；

4\.具有n nn个结点的m mm叉树的最小高度为\[logm\(n\(m\-1\)\+1\)\]

## 4\.树的存储结构

1\.双亲表示法

我们假设以一组连续空间存储树的结点，同时在每个结点中，附设一个指示器指示其双亲结点到链表中的位置。也就是说，每个结点除了知道自已是谁以外，还知道它的双亲在哪里。

```c++
typedef struct PTNode{
        int data;        //结点数据
        int parent;        //双亲位置
}PTNode;
/*树结构*/
typedef struct{
        PTNode nodes[100];        //结点数组
        int r, n;        //根的位置和结点数
}PTree;
```

2\.孩子表示法

具体办法是，把每个结点的孩子结点排列起来，以单链表作存储结构，则n个结点有n个孩子链表，如果是叶子结点则此单链表为空。然后n个头指针又组成\-一个线性表，采用顺序存储结构，存放进一个一维数组中，如图所示。

![image.png](/img/posts/data-structures/image%2016.png)

```c++
/*树的孩子表示法结构定义*/
#define MAX_TREE_SIZE 100
/*孩子结点*/
typedef struct CTNode{
        int child;
        struct CTNode *next;
}*ChildPtr;
/*表头结点*/
typedef struct{
        TElemType data;
        ChildPtr firstchild;
}CTBox;
/*树结构*/
typedef struct{
        CTBox nodes[MAX_TREE_SIZE];        //结点数组
        int r, n;        //根的位置和结点数
}
```

3\.孩子兄弟表示法

刚才我们分别从双亲的角度和从孩子的角度研究树的存储结构，如果我们从树结点的兄弟的角度又会如何呢?当然，对于树这样的层级结构来说，只研究结点的兄弟是不行的，我们观察后发现，任意一棵树， 它的结点的第一个孩子如果存在就是唯一的，它的右兄弟如果存在也是唯一的。 因此，我们设置两个指针，分别指向该结点的第一个孩子和此结点的右兄弟。

```c++
/*树的孩子兄弟表示法结构定义*/
typedef struct CSNode{
        TElemtype data;
        struct CSNode *firstchild, *rightsib;
} CSNode, *CSTree;

```

这个表示法的最大好处就是它把一棵复杂的树变成了一棵**二叉树**。

## 5\.二叉树

### 1\.二叉树的定义

二叉树是另一种树形结构，其特点是每个结点至多只有两棵子树\( 即二叉树中不存在度大于2的结点\)，并且二叉树的子树有左右之分，其次序不能任意颠倒。

与树相似，二叉树也以递归的形式定义。二叉树是n \(n≥0\) 个结点的有限集合:

1\.或者为空二叉树，即n=0。

2\.或者由一个根结点和两个互不相交的被称为根的左子树和右子树组成。左子树和右子树又分别是一棵二叉树。

二叉树是有序树，若将其左、右子树颠倒，则成为另一棵不同的二叉树。即使树中结点只有一棵子树，也要区分它是左子树还是右子树。二叉树的5种基本形态如图所示。

![image.png](/img/posts/data-structures/image%2019.png)

2、几个特殊的二叉树

（1）斜树

所有的结点都只有左子树的二叉树叫左斜树。所有结点都是只有右子树的二叉树叫右斜树。这两者统称为斜树。

（2）满二叉树

一棵高度为h,2^h−1个结点的二叉树称为满二叉树，即树中的每层都含有最多的结点。满二叉树的叶子结点都集中在二叉树的最下一层，并且除叶子结点之外的每个结点度数均为2。可以对满二叉树按层序编号:约定编号从根结点\(根结点编号为1\)起，自上而下，自左向右。这样，每个结点对应一个编号,对于编号为i的结点，若有双亲，则其双亲为i/2,若有左孩子，则左孩子为2i;若有右孩子，则右孩子为2i \+ 1

![image.png](/img/posts/data-structures/image%2015.png)

（3）完全二叉树

高度为h hh、有n nn个结点的二叉树，当且仅当其每个结点都与高度为h hh的满二叉树中编号为1\~n的结点一一对应时，称为完全二叉树，如图所示。其特点如下:

![image.png](/img/posts/data-structures/image.png)

1. 若i ≤ n / 2, 则结点i ii为分支结点，否则为叶子结点。

2. 叶子结点只可能在层次最大的两层上出现。对于最大层次中的叶子结点，都依次排列在该层最左边的位置上。

3. 若有度为1的结点，则只可能有一个，且该结点只有左孩子而无右孩子\(重要特征\)。

4. 按层序编号后，一旦出现某结点\(编号为i\)为叶子结点或只有左孩子，则编号大于i的结点均为叶子结点。

5. 若n为奇数，则每个分支结点都有左孩子和右孩子;若n为偶数，则编号最大的分支结点\(编号为n / 2\)只有左孩子，没有右孩子，其余分支结点左、右孩子都有

（4）二叉排序树

左子树上所有结点的关键字均小于根结点的关键字;右子树上的所有结点的关键字均大于根结点的关键字;左子树和右子树又各是一棵二叉排序树

（5）平衡二叉树

树上任一结点的左子树和右子树的深度之差不超过1。

### 2\.二叉树的存储结构

（1）顺序存储结构

二叉树的顺序存储是指用一组地址连续的存储单元依次自上而下、自左至右存储完全二叉树上的结点元素，即将完全二叉树上编号为i的结点元素存储在一维数组下标为i − 1的分量中。

依据二叉树的性质，完全二叉树和满二叉树采用顺序存储比较合适，树中结点的序号可以唯一地反映结点之间的逻辑关系，这样既能最大可能地节省存储空间，又能利用数组元素的下标值确定结点在二叉树中的位置，以及结点之间的关系。

但对于一般的二叉树，为了让数组下标能反映二叉树中结点之间的逻辑关系，只能添加一些并不存在的空结点，让其每个结点与完全二叉树上的结点相对照，再存储到一维数组的相应分量中。然而，在最坏情况下，一个高度为h且只有h个结点的单支树却需要占据近2h − 1个存储单元。二叉树的顺序存储结构如图所示，其中0表示并不存在的空结点

![image.png](/img/posts/data-structures/image%203.png)

（2）链式存储结构

既然顺序存储适用性不强，我们就要考虑链式存储结构。二叉树每个结点最多有两个孩子，所以为它设计一个数据域和两个指针域是比较自然的想法，我们称这样的链表叫做二叉链表。

### 3\.二叉树的遍历

![image.png](/img/posts/data-structures/image%201.png)

1、先序遍历

先序遍历\(PreOrder\) 的操作过程如下:
若二叉树为空，则什么也不做，否则，
1\)访问根结点;
2\)先序遍历左子树;
3\)先序遍历右子树。

![image.png](/img/posts/data-structures/image%204.png)

```c++
void PreOrder(BiTree T){
        if(T != NULL){
                visit(T);        *//访问根节点*
                PreOrder(T->lchild);        *//递归遍历左子树*
                PreOrder(T->rchild);        *//递归遍历右子树*
        }
}
```

2\.中序遍历

中序遍历\( InOrder\)的操作过程如下：
若二叉树为空，则什么也不做，否则，

1\)中序遍历左子树;
2\)访问根结点;
3\)中序遍历右子树。

![image.png](/img/posts/data-structures/image%2010.png)

3\.后序遍历

后序遍历\(PostOrder\) 的操作过程如下：
若二叉树为空，则什么也不做，否则，
1\)后序遍历左子树;
2\)后序遍历右子树;
3\)访问根结点

![image.png](/img/posts/data-structures/image%2020.png)

### 4\.二叉树的建立

![image.png](/img/posts/data-structures/image%2014.png)

![image.png](/img/posts/data-structures/image%2022.png)

## 6\.树，森林，二叉树的转换

### 1\.树转为二叉树

![image.png](/img/posts/data-structures/image%208.png)

### 2\.森林转为二叉树

![image.png](/img/posts/data-structures/image%2017.png)

### 3\.二叉树转为树

![image.png](/img/posts/data-structures/image%209.png)

### 4\.二叉树转为森林

![image.png](/img/posts/data-structures/image%2011.png)

## 7\.赫夫曼树

### 1。赫夫曼树的定义和原理

在许多应用中，树中结点常常被赋予一个表示某种意义的数值，称为该结点的权。从树的根到任意结点的路径长度\(经过的边数\)与该结点上权值的乘积，称为该结点的带权路径长度。树中所有叶结点的带权路径长度之和称为该树的带权路径长度。

在含有n个带权叶结点的二叉树中，其中带权路径长度\(WPL\)最小的二叉树称为哈夫曼树，也称最优二叉树。例如，下图中的3棵二叉树都有4个叶子结点a, b,c,d,分别带权7,5,2,4，它们的带权路径长度分别为

![image.png](/img/posts/data-structures/image%207.png)

a\. WPL = 7x2 \+ 5x2 \+ 2x2 \+ 4x2 = 36。
b\. WPL = 4x2 \+ 7x3 \+ 5x3 \+ 2x1 = 46。
c\. WPL = 7x1 \+ 5x2 \+ 2x3 \+ 4x3 = 35。
其中，图c树的WPL最小。可以验证，它恰好为哈夫曼树。

### 2\.赫夫曼树的构造

![image.png](/img/posts/data-structures/image%202.png)

# 7\.图

## 1\.prim算法\(求最小生成树\)

力扣：[连接所有点的最小费用](https://leetcode.cn/problems/min-cost-to-connect-all-points/)；

```java
int minCostConnectPoints(int** points, int pointsSize, int* pointsColSize) {
    int sum = 0;
    int arr[1010][1010] = {0};
    for (int i = 0; i < pointsSize - 1; i++){
        for (int j = i + 1; j < pointsSize; j++){
            arr[i][j] = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1]);
            arr[j][i] = arr[i][j];
        }
    }
    int l[1010] = {0};
    int v[1010] = {0};
    v[0] = 1;
    for (int i = 1; i < pointsSize; i++){
        l[i] = arr[0][i];
    }
    for (int i = 1; i < pointsSize; i++){
        int min = 99999999;
        int temp = -1;
        for (int j = 0; j < pointsSize; j++){
            if(l[j] < min && v[j] == 0){
                min = l[j];
                temp = j;
            }
        }
        sum += min;
        v[temp] = 1;
        for (int j = 0; j < pointsSize; j++){
            if(arr[temp][j] < l[j] && v[j] == 0){
                l[j] = arr[temp][j];
            }
        }
    }
    return sum;
}
```

## 2\.Kruskal

题目如1\.

```c++
struct s{
    int st;
    int en;
    int w;
}s1[1000010];
int cmp (const void* a, const void* b){
    return ((struct s*)a)->w - ((struct s*)b)->w;
}
int jud(int i,int p[]){
    while(p[i] > 0){
        i = p[i];
    }
    return i;
}
int minCostConnectPoints(int** points, int pointsSize, int* pointsColSize) {
    int sum = 0;
    int cnt = 0;
    int arr[1010][1010] = {0};
    for (int i = 0; i < pointsSize - 1; i++){
        for (int j = i + 1; j < pointsSize; j++){
            arr[i][j] = abs(points[i][0] - points[j][0]) + abs(points[i][1] - points[j][1]);
            s1[cnt].w = arr[i][j];
            s1[cnt].st = i;
            s1[cnt++].en = j;
        }
    }
    qsort(s1,cnt,sizeof(struct s),cmp);
    int p[1010] = {0};
    for (int i = 0; i < cnt; i++){
        int a = jud(s1[i].st,p);
        int b = jud(s1[i].en,p);
        if(a != b){
            sum += s1[i].w;
            p[a] = b;
        }
    }
    return sum;
}
```

## 3\.dijkstra

力扣\(网络延迟时间\):https://leetcode\.cn/problems/network\-delay\-time/

```java
nt networkDelayTime(int** times, int timesSize, int* timesColSize, int n, int k) {
    int arr[110][110] = {0};
    memset(arr,-1,sizeof(arr));
    int book[110] = {0};
    int l[110] = {0};
    for(int i = 0; i < timesSize; i++){
        arr[times[i][0]][times[i][1]] = times[i][2];
    }
    for(int i = 1; i <= n - 1; i++){
        for (int j = i + 1; j <= n; j++){
            if(arr[i][j] == -1){
                arr[i][j] = 99999;
            }
            if(arr[j][i] == -1){
                arr[j][i] = 99999;
            }
        }
    }
    for (int i = 1; i <= n; i++){
        arr[i][i] = 0;
    }
    for (int i = 1; i <= n; i++){
        l[i] = arr[k][i];
    }
    book[k] = 1;
    for (int i = 1; i <= n - 1; i++){
        int min = 99999;
        int index = 0;
        for (int j = 1; j <= n; j++){
            if(book[j] == 0 && l[j] < min){
                min = l[j];
                index = j;
            }
        }
        book[index] = 1;
        if(index == 0){
            continue;
        }
        for (int j = 1; j <= n; j++){
            if(l[j] > l[index] + arr[index][j]){
                l[j] = l[index] + arr[index][j];
            }
        }
    }
    int time = 0;
    for (int i = 1; i <= n; i++){
        if(book[i] == 0){
            return -1;
        } else {
            if(time < l[i]){
                time = l[i];
            }
        }
    }
    return time;
}
```

## 4\.floyd

力扣:[1334\. 阈值距离内邻居最少的城市](https://leetcode.cn/problems/find-the-city-with-the-smallest-number-of-neighbors-at-a-threshold-distance/)

```java
#define  INF 999999999
int findTheCity(int n, int** edges, int edgesSize, int* edgesColSize, int distanceThreshold) {
    int arr[110][110] = {0};
    for (int i = 0; i < n; i++){
        for (int j = 0; j < n; j++){
            if(i != j){
                arr[i][j] = INF;
            }
        }
    }
    for (int i = 0; i < edgesSize; i++){
        arr[edges[i][0]][edges[i][1]] = edges[i][2];
        arr[edges[i][1]][edges[i][0]] = edges[i][2];
    }
    int s[110][110] = {0};
    for (int i = 0 ; i < n; i++){
        for (int j = 0; j < n; j++){
            s[i][j] = arr[i][j];
        }
    }
    for(int i = 0; i < n; i++){
        for (int j = 0; j < n; j++){
            for (int l = 0; l < n; l++){
                if(s[j][i] + s[i][l] < s[j][l]){
                    s[j][l] = s[j][i] + s[i][l];
                }
            }
        }
    }
    int min = 9999;
    int index = -1;
    for (int i = 0; i < n; i++){
        int cnt = 0;
        for (int j = 0; j < n; j++){
            if(s[i][j] != 0 && s[i][j] <= distanceThreshold){
                cnt++;
            }
        }
        printf("%d", cnt);
        if (min >= cnt){
            min = cnt;
            index = i;
        }
    }
    return index;
}
```

## 5\.拓扑排序

力扣:[207\. 课程表](https://leetcode.cn/problems/course-schedule/)

```C
struct node{
    int index;
    struct node* next;
};
struct vertexnode{
    int in;
    struct node* head;
}vertexnode[2100];
void delete(int book[2100],int index){
    struct node* head = vertexnode[index].head;
    while(head-> next != NULL){
        vertexnode[head->next->index].in--;
        if(vertexnode[head->next->index].in == 0 && book[head->next->index] == 0){
            book[head->next->index] = 1;
            delete(book,head->next->index);
        }
        head = head->next;
    }
}
bool canFinish(int numCourses, int** prerequisites, int prerequisitesSize, int* prerequisitesColSize) {
    int book[2100] = {0};
    memset(vertexnode,0,sizeof(vertexnode));
    for (int i = 0; i < numCourses; i++){
        vertexnode[i].head = (struct node*)calloc(1,sizeof(struct node));
    }
    for (int i = 0 ; i < prerequisitesSize; i++){
        struct node* s = (struct node*)calloc(1,sizeof(struct node));
        int a = prerequisites[i][1];
        int b = prerequisites[i][0];
        s -> index = b;

        struct node* head = vertexnode[a].head;
        vertexnode[b].in++;
        while(head->next != NULL){
            head = head->next;
        }
        head->next = s;
    }
    for (int i = 0; i < numCourses; i++){
        if(vertexnode[i].in == 0 && book[i] == 0){
            book[i] = 1;
            delete(book,i);
        }
    }
    for (int i = 0; i < numCourses; i++){
        if(book[i] == 0){
            return 0;
        }
    }
    return 1;
}
```

# 8\.查找

## 1\.斐波那契查找（有序表查找）

通过斐波那契数组的黄金比例进行分割处理,在求mid值时使用了加减法而未用乘除，一定程度能优化查找的性能。

```c++
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
using namespace std;
#include <stdlib.h>
#include <stdio.h>
int f[100] = { 0 };
void get(int n) {
    f[0] = 1;
    f[1] = 1;
    for (int i = 2; i <= n; i++) {
        f[i] = f[i - 1] + f[i - 2];
    }
}
int main()
{
    get(30);
    int n = 10;//假设n为10;
    int arr[16] = {12,15,16,19,24,25,29,34,35,40};
    int key;
    scanf("%d", &key);
    int k = 0;
    while (n > f[k] - 1) {
        k++;
    }
    int y = n;
    while (y <= f[k] - 1) {//补全数组，使其变为f[k] - 1,变为f[k] - 1是为了后面二分后使左右两边两组的数量依然为f[k]-1
        arr[y++] = arr[y - 1] + 1;
    }
    int l = 0;
    int r = n-1;
    while (l <= r) {
        int mid = l + f[k] - 1;//通过斐波那契数组求出mid值
        if (arr[mid] < key) {
            l = mid + 1;
            k -= 2;
        } else if (arr[mid] > key){
            r = mid - 1;
            k -= 1;
        } else if(arr[mid] == key){
            if (mid >= n) {//找到的数为补全用的数
                printf("-2");
                return 0;
            }
            else {
                printf("%d", mid);
                return 0;
            }
        }
    }
    printf("-1");
    return 0;
}
```

# 9\.排序

## 1\.直接插入排序

从第二个数遍历到最后一个数,把每一个数记为temp与前面所有数比较，若小于前面的数则将较大的数向前移动一位，并将temp放在此位置，反之直接break。

```c++
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main() {
        int length = 10;
        int arr[10] = { 9,3,5,2,1,4,6,8,7,10 };
        for (int i = 1; i < length; i++) {
                int temp = arr[i];
                for (int j = i - 1; j >= 0; j--) {
                        if (temp < arr[j]) {
                                arr[j + 1] = arr[j];
                                arr[j] = temp;
                        } else {
                                break;
                        }
                }
                for (int i = 0; i < 10; i++) {
                        printf("%d", arr[i]);
                }
                printf("\n");
        }
}
```

**1\.时间复杂度**

最好情况就是全部有序，此时只需遍历一次，最好的时间复杂度为O \(n\)

最坏情况全部反序，内层每次遍历已排序部分，最坏时间复杂度为O\(n^2）

**2\.空间复杂度**

平均的空间复杂度为：O \( 1 \)

## 2\.希尔排序\(优化版的直接插入排序\)

直接插入排序是序列越有序\(每个数之前的所有大于这个数的个数的和越小越有序\)则排序越快,

而希尔排序则是将序列变的有序在进行直接插入排序，从而使直接插入排序的排序速度更快

```c++
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int main() {
        int length = 10;
        int x = length;
        int gap;//组距
        int arr[10] = { 9,3,5,2,1,4,6,8,7,10 };
        while (x > 0) {
                x /= 2;
                gap = x;
                for (int i = gap; i < length; i++) {
                        int temp = arr[i];
                        for (int j = i - gap; j >= 0; j -= gap) {
                                if (temp < arr[j]) {
                                        arr[j + gap] = arr[j];
                                        arr[j] = temp;
                                }
                                else {
                                        break;
                                }
                        }
                }
                for (int i = 0; i < 10; i++) {
                        printf("%d", arr[i]);
                }
                printf("\n");
        }
}
```

**1\.最坏情况**O\(n²\)

当采用**较差的增量序列**​（如原始希尔增量序列：每次取数组长度的一半并递减）时，希尔排序可能退化为接近插入排序的最坏时间复杂度。

例如，当增量序列为 `[..., 8, 4, 2, 1]` 时，若数据分布极不均匀，可能导致大量比较和移动操作

2\.**平均情况**：O\(n^1\.3\) \~ O\(n log² n\)

3\.**最好情况**：O\(n log n\)

当数据已基本有序且采用高效增量序列时，希尔排序的复杂度接近线性对数级

## 3\.堆排序

堆排序就是将所有要排列的数当成一个完全二叉数\(除最后一层外，其它层数的节点达到了最大个数，且最后一层的节点从左到右连续\)，先将待排序的数组构造成一个大根堆，此时，整个数组的最大值就是堆结构的顶端将顶端的数与末尾的数交换，此时，末尾的数为最大值，剩余待排序数组个数为n\-1，将剩余的n\-1个数再构造成大根堆，再将顶端数与n\-1位置的数交换，如此反复执行，便能得到有序数组。

**大根堆和小跟堆**

性质：每个结点的值都大于其左孩子和右孩子结点的值，称之为大根堆；每个结点的值都小于其左孩子和右孩子结点的值，称之为小根堆。如下图

![image.png](/img/posts/data-structures/image%205.png)

代码如下

![image.png](/img/posts/data-structures/image%2013.png)

![image.png](/img/posts/data-structures/image%206.png)

堆排序的时间复杂度无论什么情况下都是O\(n log n\)，但是因为常数因子过大，所以某些情况下执行速度可能不如快速排序

## 4\.归并排序

**实现算法：分治算法**

![image.png](/img/posts/data-structures/image%2021.png)

**先将总序列分成一个个单一序列，再通过单一序列间的排序将其归并起来**

![image.png](/img/posts/data-structures/image%2018.png)

代码如下

```sql
#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <string.h>
void merge(int arr[],int temp[], int start, int end) {
    int mid = (start + end) / 2;
    if (start < end) {
         merge(arr, temp, start, mid);
         merge(arr, temp, mid + 1, end);
    }
    int r = start;
    int l = mid + 1;
    int cnt = start;
    while (r != mid + 1 && l != end + 1) {
        if (arr[r] > arr[l]) {
            temp[cnt++] = arr[r];
            r++;
        } else {
            temp[cnt++] = arr[l];
            l++;
        }
    }
    while (r != mid + 1) {
        temp[cnt++] = arr[r++];
    }
    while (l != end + 1) {
        temp[cnt++] = arr[l++];
    }
    for (int i = start; i <= end; i++) {
        arr[i] = temp[i];
    }
}
int main()
{
    int a[10] = { 1,4,3,5,2,6,8,7,9,10 };
    int b[10] = { 0 };
    merge(a, b, 0, 9);
    for (int i = 0; i < 10; i++) {
        printf("%d ", a[i]);
    }
    return 0;
}
```

堆排序的时间复杂度无论什么情况下都是O\(n log n\)，正常情况下不如快排

空间复杂度O\(n\)
